name: 'Hello Shell Action'
description: 'A simple action that prints a greeting using shell'
author: 'Doyin Elugbadebo'

inputs:
  name:
    description: 'Name to greet'
    required: true
    default: 'World'

runs:
  using: 'composite'
  steps:
    - run: echo "Hello, ${{ inputs.name }}! 👋"
      shell: bash
